version: "3.3"

services:
  nginx:
    build: .
    labels:
      - traefik.http.routers.ki-site.rule=Host(`www.ki.fh-swf.de`)

    networks:
      - traefik_default

networks:
  traefik_default:
    external: true
